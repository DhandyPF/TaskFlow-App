PORT=8080
DATABASE_DSN=./taskflow.db
JWT_SECRET=change-this-secret-in-production

# Comma-separated list of frontend origins allowed to call this API,
# e.g. https://taskflow.vercel.app,https://www.taskflow.com
# Leave empty (or unset) during local development to allow any origin.
ALLOWED_ORIGINS=
